<UserControl x:Class="FQE.AdminClient.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="Transparent">
    <Grid Margin="28">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380" />
            <ColumnDefinition Width="28" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{StaticResource PanelBrush}"
                CornerRadius="28"
                Padding="28">
            <StackPanel>
                <TextBlock Text="Acceso admin"
                           FontSize="24"
                           FontWeight="Bold" />
                <TextBlock Margin="0,8,0,16"
                           Foreground="{StaticResource MutedBrush}"
                           Text="Ingresa con tu cuenta." />

                <StackPanel>
                    <TextBlock Text="Correo" />
                    <TextBox x:Name="LoginEmailTextBox" />
                </StackPanel>

                <StackPanel Margin="0,12,0,0">
                    <TextBlock Text="Contraseña" />
                    <PasswordBox x:Name="LoginPasswordBox" />
                </StackPanel>

                <Button x:Name="LoginButton"
                        Click="LoginButton_Click"
                        Content="Entrar como admin"
                        Margin="0,20,0,0" />

                <TextBlock x:Name="LoginStatusText"
                           Margin="0,18,0,0"
                           TextWrapping="Wrap"
                           Foreground="{StaticResource MutedBrush}" />
            </StackPanel>
        </Border>

        <Border Grid.Column="2"
                Background="{StaticResource PanelBrushAlt}"
                CornerRadius="28"
                Padding="32">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Text="FigurasQE Admin"
                               FontSize="30"
                               FontWeight="Bold" />

                </StackPanel>


            </Grid>
        </Border>
    </Grid>
</UserControl>