<Application x:Class="FQE.AdminClient.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FQE.AdminClient">
    <Application.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#11243A" />
        <SolidColorBrush x:Key="PanelBrushAlt" Color="#16314D" />
        <SolidColorBrush x:Key="AccentBrush" Color="#F59E0B" />
        <SolidColorBrush x:Key="TextBrush" Color="#E5EEF8" />
        <SolidColorBrush x:Key="MutedBrush" Color="#9CB3C9" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="42" />
            <Setter Property="Margin" Value="0,6,0,0" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Background" Value="#EAF2FB" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="42" />
            <Setter Property="Margin" Value="0,6,0,0" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Background" Value="#EAF2FB" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="44" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="Margin" Value="0,8,0,0" />
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="#111827" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="42" />
            <Setter Property="Margin" Value="0,6,0,0" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Background" Value="#EAF2FB" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#10243B" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#1F3B57" />
            <Setter Property="VerticalGridLinesBrush" Value="#1F3B57" />
            <Setter Property="RowBackground" Value="#10243B" />
            <Setter Property="AlternatingRowBackground" Value="#132A43" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="CanUserResizeRows" Value="False" />
            <Setter Property="IsReadOnly" Value="True" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#16314D" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="#1F3B57" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="10,8" />
        </Style>
    </Application.Resources>
</Application>
